# DRAFT ONLY — replace id/base_url and validate against a live node before registration.
# WeatherAPI chance_of_rain is retained with explicit unverified event-equivalence
# metadata internally until historical validation proves it matches our event.
version: "1"
kind: miner
id: 0
slug: oathcast-weatherapi
protocol: generic
name: OathCast WeatherAPI Forecaster
description: WeatherAPI-backed one-hour rain probability with deterministic text output.
base_url: https://REPLACE-weatherapi-adapter.example

auth:
  type: bearer
  header_name: Authorization
  value_prefix: "Bearer "
  env_var: OATHCAST_MINER_API_KEY

endpoints:
  - path: /predict
    external_path: /v1/forecast/point
    method: GET
    description: Forecast one UTC hour without cross-hour probability aggregation.
    param_map:
      lat: latitude
      lon: longitude
      start: horizon_start
      end: horizon_end

semantics:
  signal_mapping:
    confidence_field: probability
    reason_field: content
  supported_intents:
    - WEATHER_FORECAST
    - WEATHER_CHECK

on_chain:
  transform: direct
  min_price_usdc: 0.01
  fields:
    strings:
      - index: 0
        name: content
        source_path: content
    integers:
      - index: 0
        name: probability_x10000
        source_path: probability
        multiplier: 10000

rate_limit_per_sec: 2
cache_ttl_sec: 0
circuit_threshold: 5
circuit_cooldown_seconds: 30
