# Telegraph authority matrix

Last reviewed: 2026-08-17

This matrix prevents the protocol whitepaper, Hackathon 1 instructions, and
local engineering assumptions from being merged into one unverified contract.

| Topic | Whitepaper / protocol concept | Hackathon 1 authority | OathCast implementation rule |
|---|---|---|---|
| Miner registration | Permissionless on-chain registration; the whitepaper describes Intent IDs, fee address, price floor, credential/schema metadata, and output mapping. | Registration is live and permissionless. The live portal validates YAML, sandbox-tests endpoints, pins the YAML, and submits `registerMiner` on Base Sepolia. | Keep the raw-YAML digest and transaction inputs explicit. Recheck slug availability before requesting confirmation to connect/sign; inspect the numeric routing ID conservatively, but do not confuse it with the sequential on-chain `registrationId` or treat it as an official uniqueness gate. Never infer registration from a local validator pass. |
| Miner price | 0.01 USDC protocol floor in the whitepaper. | Live docs set the minimum at `10000` micro-USDC; price is a registration transaction input, not required inside the Miner YAML. | Store and validate integer micro-USDC at the registration boundary; default the unsigned draft to the exact floor. |
| Payment | x402 challenge, wallet authorization, Telegraph verification, routing, and receipt. | Current official docs list x402 v2 payment through the HTTPS dev node with Base Sepolia USDC or Solana Devnet USDC. The exact `accepts[]` entry in the received 402 challenge is authoritative for that request. | Keep using the dedicated testnet payment boundary. Pin network, asset, amount, recipient, fee payer when present, URL, Miner path, and payload before signing; independently verify settlement and retain the returned signal identity. |
| Protocol receipt | Cryptographic Signal Receipt after protocol finalization. | Current docs return `signal_hash` for a paid call and expose `GET /engine/v1/signal/{signal_hash}` for verification. Explorer visibility remains corroboration; Telegraph-side accounting is authoritative for qualification. | Keep OathCast forecast receipts, x402 artifacts, settlement evidence, and Telegraph signal records as separate linked evidence types. |
| Script Author | Permissionless WASM registration and sandbox constraints; the whitepaper mentions a Machina bond. | The current scoring guide requires exported linear memory plus `alloc(i32)->i32`, `dealloc(i32,i32)`, and `rank_answer(6 x i32)->f32`; inputs are UTF-8 pointer/byte-length pairs, blank answers score `0`, output is `[0,1]`, the standalone artifact is at most 32 MiB, and sandboxed code has no network/filesystem/shared state. `breakdown_answer` is deprecated and removed. Registration costs gas only. Live portal build `D8HL6V9WUTFV9A7Ryk0W0` targets `0x5a2324aA18613FAD4e44bDF0d6c73Ec1f6D87ff8` and calls `registerWasm(bytes32,string,string)`, selector `0xfe1e40f7`, transmitting the canonical Intent. User-relayed Telegraph guidance says Stage 2 uses factual-paraphrase and lexical-discrimination fixtures, a fixed `0.15` floor for each of six near-miss cases, and automatic promotion after all six pass. Candidate and champion aggregate margins are not directly compared. The reported `0.60` metric is Spearman correlation against the live champion's historical scores. | OathCast preserves current and historical artifact states. The 16,292-byte artifact (SHA-256 `97d481b724bd79fa78d32218f20be9c1b85468109a8ff2a0da2d2574c775f3af`, raw Keccak `0xea169bc97fc43c3de086d26765714a28c909d29a6d79181f93d2f9e236776ab8`) remains proven at current-registry ID `7` and was evaluated as registration `19` from wallet `0x7dc9C9D535B68C3c6273e3323f0e52E5851C3278` in transaction `0xa6bc6f653eec4a5c79acac4a6e747222d48fd257367c325cd0e6c0090d321e73`; Stage 1 passed and Stage 2 rejected it at `31/32` ordering wins versus the champion's `32/32`, with margins `0.31248063` and `0.37360683` and zero historical rows. The two obsolete-registry transactions and ID `7` remain historical chronology. The current 42,790-byte artifact (SHA-256 `2c1f7ad3ec409d91a778a3d49a6d554de09bc12701834fd859f07591550a0774`, raw Keccak `0xe217913a8a22b2d80b607008b3605e45b646e624b56005f1df84925e9818e47a`, fixture SHA-256 `c96960e6a5e0d0d410686bcf9a2c0dece48ec130e19403322355f19ca4096b0f`) has two isolated byte-identical builds and passes Rust `40/40`, the full Go/wazero suite, and Python discovery `498/498`. It passes 88 synthetic factual pairs with minimum margin `0.206250` and synthetic ordinal Spearman `0.959566`; these are local proxies only, and it has not been uploaded, hosted, or registered. Registration `41` registered the earlier 42,798-byte artifact (SHA-256 `4c3e91ac887abf492cbc662a2d02e0b0bae906a176b2ae4b7bf986419a2db174`, raw Keccak `0xd8b298ded6e50a69fd6cc79350a819536927d879c81250924689edbea98517f8`), which passed Telegraph's unmodified tester (`0.8500`). The user manually hosted that exact 42,798-byte artifact and completed registration `41` for `WEATHER_FORECAST` in transaction `0x4bfdc7a894ca55edbb18c18cd5ee79b32673c8b3f5b8d04ab6bc5e48a458ccf8`; independent hosted-byte verification matched the size and hashes. Registration `41` reached champion comparison, establishing Stage 1 passage, then failed Stage 2 at `31/32` candidate wins versus the champion's `32/32`. Candidate margin/EvalScore `0.37852418` exceeded the champion aggregate margin `0.37360683`, but the higher aggregate did not override the failed per-case promotion rule. No further registration is authorized. Any future attempt requires a fresh decoded wrapper/nested-call preflight and fresh explicit user authorization. |
| Flow vs Workflow | Paid Intent flow ends in routing/validation/receipt; stateful orchestration belongs to the client. | Track 3 must use real Telegraph Miners and genuine requests. | Keep payment/routing in the protocol adapter and case state, decisions, and outcome resolution in the Application. |
| Mainnet / testnet | Phase 1 Base Sepolia, Phase 2 Base mainnet. | Miner registration is on Base Sepolia. Current x402 docs offer Base Sepolia and Solana Devnet payment choices; both are test environments and the received challenge determines the usable route. | No mainnet wallet, mainnet contract, escrow, or MPC path is enabled. Use only a dedicated testnet wallet for explicitly authorized paid canaries. |

Sources: [Telegraph Protocol whitepaper](https://telegraphprotocol.com/Whitepapers%20-%20Telegraph%20Protocol.pdf), [official hackathon rules](https://hackathon.telegraphprotocol.com/rules), [current Telegraph docs](https://docs.telegraphprotocol.com/docs), and the [Miner registration portal](https://integrate.telegraphprotocol.com/).
